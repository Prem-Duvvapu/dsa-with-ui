<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<parent>
		<groupId>org.springframework.boot</groupId>
		<artifactId>spring-boot-starter-parent</artifactId>
		<version>3.2.3</version>
		<relativePath/> <!-- lookup parent from repository -->
	</parent>
	<groupId>com.dsa</groupId>
	<artifactId>dsa-backend</artifactId>
	<version>1.0.0</version>
	<name>dsa-backend</name>
	<description>Spring Boot Backend for DSA with UI (Striver A2Z Visualizer)</description>

	<properties>
		<java.version>17</java.version>
	</properties>

	<dependencies>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-web</artifactId>
		</dependency>

		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-test</artifactId>
			<scope>test</scope>
		</dependency>
	</dependencies>

	<build>
		<plugins>
			<plugin>
				<groupId>org.springframework.boot</groupId>
				<artifactId>spring-boot-maven-plugin</artifactId>
			</plugin>
			<!--
				Coverage reporting only — no threshold. There was no coverage tooling on
				either side, so the first job is to see the number before arguing about it.
				`mvn test` writes target/site/jacoco/index.html.

				The exec file lives outside target/ on purpose. At JVM exit the agent
				re-opens the exec file under a FileChannel lock, and on WSL that call
				intermittently hangs for 30+ seconds when the project sits on /mnt/c —
				surefire's watchdog then kills the fork ("Surefire is going to kill self
				fork JVM"). On the native filesystem the lock is instant. Both the agent
				and the report goal read this path, and CI is unaffected.
			-->
			<plugin>
				<groupId>org.jacoco</groupId>
				<artifactId>jacoco-maven-plugin</artifactId>
				<version>0.8.12</version>
				<configuration>
					<destFile>${java.io.tmpdir}/dsa-backend-jacoco.exec</destFile>
					<dataFile>${java.io.tmpdir}/dsa-backend-jacoco.exec</dataFile>
				</configuration>
				<executions>
					<execution>
						<id>prepare-agent</id>
						<goals>
							<goal>prepare-agent</goal>
						</goals>
					</execution>
					<execution>
						<id>report</id>
						<phase>test</phase>
						<goals>
							<goal>report</goal>
						</goals>
					</execution>
				</executions>
			</plugin>
		</plugins>
	</build>
</project>
